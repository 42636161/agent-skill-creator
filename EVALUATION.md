# EVALUATION — agent-skill-creator Quality Framework v2.0

> 本评估框架对 agent-skill-creator 生成的 skill 做系统质量审计。原始发现
> 来自一次完整的 CRM 业务生成周期，但本文件已将根因归纳为框架级质量维度，
> 不再依附于具体领域或 BUG ID。
>
> **核心变化**：每个维度同时衡量对 **AI 利用力** 的影响——即 agent 理解、
> 执行、自我修正该 skill 的效率与可靠性。

---

## 质量维度

### 1. AI 可解读性 — 隐含假设无机器可读表达

**问题**：pipeline 代码承载大量隐含假设——时间窗口语义、字段多义性、未调用
函数——这些假设不对 agent 可见。agent 被迫自行推测上下文语义，导致行为不一致、
context 浪费、跨领域场景出现矛盾输出。

**AI 利用力影响**：高。agent 每次调用需重新猜测隐含约束，不仅消耗 context
窗口，还可能得出与用户预期矛盾的结论。当假设不成立时，agent 无法自检。

**根因**：不存在机器可读的假设声明层。validate.py 只检查 frontmatter 格式
与目录结构，不涉及代码语义。

### 2. 执行可靠性 — 步骤依赖与容错缺失

**问题**：pipeline 步骤依赖（如 clean → report）写在 SKILL.md 的自然语言
描述中，pipeline 却接受独立 flags 调用。agent 调用 `--report` 时不自动触发
`--clean`，输出依赖错误结果。同时 I/O 层无容错保护：编码不匹配、列名漂移、
目录不存在、NULL 值——全部直接崩溃。

**AI 利用力影响**：高。agent 无法自行推导 DAG 顺序，必然输出错误结果。边缘
场景崩溃迫使 agent 切换至调试模式，自动化率显著下降。

**根因**：DAG 以文档约束而非代码表达。I/O 代码缺乏防御性编程。

### 3. 验证完备性 — 轻量包装 vs 深层缺陷

**问题**：validate.py 检查 frontmatter 完整性、line count、目录结构，但不
检查死函数、死变量、字段语义一致性、硬编码常量。质量门是格式化门而非语义门。

**AI 利用力影响**：中高。浅层验证放行的缺陷（死函数、魔法数字）会被 agent
执行时遇到，浪费 context 处理无效代码。语义不一致问题直到用户反馈才暴露，
修复成本高于检出阶段。

**根因**：验证工具未使用 AST 层分析。代码合规性检查未与语义检查分层。

### 4. 测试有效性 — Golden case 覆盖深度不足

**问题**：Golden case 数据量小，仅覆盖 happy path。边界值（NULL、空串、超长、
特殊字符、0/负数、日期格式）无测试。首次 promote 时无历史 baseline，regression
gate 首次回滚无法检出退化。

**AI 利用力影响**：中（执行时低，优化时高）。一次执行中 happy path 覆盖大部分
场景。但在优化/演化循环中 agent 缺乏足够回归信号，边界值变更可能导致静默退化。

**根因**：合成策略以正常输入为主，未使用边界模板或组合测试策略。

---

## 优化建议（按 AI 利用力重排序）

### P0 — 这些缺陷直接阻碍 agent 正确使用 skill

| # | 建议 | 维度 | 工作量估算 |
|---|---|---|---|
| 1 | **Phase 5 追加 `contract.json`**：声明输入编码/列名假设、输出字段列表、步骤 DAG、字段语义。validate 检查代码与 contract 一致性。AI 利用力增益：agent 读取 contract 即理解隐含假设，无需从自然语言推测。 | 可解读性 | 2-3 天 |
| 2 | **DAG 依赖迁入 pipeline 代码**：入口只接受最终目标（`--report`），engine 自动补全前置步骤。AI 利用力增益：agent 不再需要理解步骤顺序。 | 执行可靠性 | 0.5 天 |
| 3 | **AGENTS.md 追加 `## Agent Constraints` 节**：至少 3 条 Do NOT / Must 子句。validate 检查存在性。AI 利用力增益：给 agent 明确行为边界。 | 可解读性 | 0.5 天 |

### P1 — 降低 agent 执行效率，建议下一迭代解决

| # | 建议 | 维度 | 工作量估算 |
|---|---|---|---|
| 4 | **validate.py 增加 AST 级检查**：死函数、死变量、魔法数字。只用标准库 ast 模块。AI 利用力增益：减少 agent 处理无效代码的 context 开销。 | 验证完备性 | 1-2 天 |
| 5 | **I/O 模板增加防御性编程**：编码自动检测、列名模糊匹配、目录自动创建、NULL 安全处理。AI 利用力增益：减少边缘场景的调试中断。 | 执行可靠性 | 1-2 天 |
| 6 | **`--check-universal` 纳入默认 pipeline lint 输出**：非阻塞性告警。保障 universal 模式下技能纯度。 | 验证完备性 | 0.5 天 |

### P2 — 提升优化/演化循环效果

| # | 建议 | 维度 | 工作量估算 |
|---|---|---|---|
| 7 | **Golden case 边界模板扩增**：每个字段的 NULL、空串、超长、特殊字符、边界日期各一条。 | 测试有效性 | 1 天 |
| 8 | **扩展 AST 至完整静态分析**：导入链、未定义引用、类型冲突的规则检查。 | 验证完备性 | 3-5 天 |

---

## 路线图排除

以下方向经评估确认不纳入核心改进路径：

- **ML 预测质量**：cross-skill 数据量不足以训练可靠模型，优先用确定性验证。
- **Agent 约束自然语言化**：约束必须保持机器可验证，自然语言仅作补充说明。
- **按领域建独立模板**：维护成本 > 收益，领域差异更适合在 contract 中表达。

---

## 版本记录

| 版本 | 日期 | 变化 |
|---|---|---|
| v2.0 | 2026-07-30 | 按质量维度重组，新增 AI 利用力分析，按利用力重排优化建议优先级，迁移"不推荐"至路线图排除 |
| v1.0 | 2026-07-30 | 初始版本，基于 CRM 完整生成周期——5 项缺陷 + P0/P1/P2 建议 |

*下次评估触发条件：下一个跨领域 skill 的完整测试周期，或 universal mode 产出的首个完整测试周期。*
